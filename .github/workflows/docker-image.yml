name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}

    - name: Build and push Docker image
      run: |
        docker build -t tunahanyilmaz/petclinic:latest .
        docker push tunahanyilmaz/petclinic:latest

    - name: Authorization with config file
      uses: actions-hub/kubectl@master
      env:
        KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
      with:
        args: -f petclinic-deployment.yaml --insecure-skip-tls-verify 
              -f petclinic-service.yaml --insecure-skip-tls-verify 
              -f postgres-deployment.yaml --insecure-skip-tls-verify 
              -f pv.yaml --insecure-skip-tls-verify 
              -f petclinic-secrets.yaml --insecure-skip-tls-verify 
              -f postgres-config.yaml --insecure-skip-tls-verify  
              -f postgres-secret.yaml --insecure-skip-tls-verify 
              -f postgres-pvc.yaml --insecure-skip-tls-verify 


   