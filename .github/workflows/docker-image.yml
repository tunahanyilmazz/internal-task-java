name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_ACCESS_TOKEN }}

    - name: Build and push Docker image
      run: |
        docker build -t tunahanyilmaz/petclinic:latest .
        docker push tunahanyilmaz/petclinic:latest

    - name: Deploy to Kubernetes
      uses: tale/kubectl-action@v1
      with:
        base64-kube-config: ${{ secrets.KUBECONFIG }}
    - run:  kubectl get pods --insecure-skip-tls-verify   
            kubectl -f petclinic-deployment.yaml --insecure-skip-tls-verify 
            kubectl -f petclinic-service.yaml --insecure-skip-tls-verify 
            kubectl -f postgres-deployment.yaml --insecure-skip-tls-verify 
            kubectl -f pv.yaml --insecure-skip-tls-verify 
            kubectl -f petclinic-secrets.yaml --insecure-skip-tls-verify 
            kubectl -f postgres-config.yaml --insecure-skip-tls-verify  
            kubectl -f postgres-secret.yaml --insecure-skip-tls-verify 
            kubectl -f postgres-pvc.yaml --insecure-skip-tls-verify 